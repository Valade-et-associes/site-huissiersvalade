---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import { formatVehicleTitle, vehicles } from "../../../data/vehicles";

export function getStaticPaths() {
  return vehicles.map((vehicle) => ({
    params: { slug: vehicle.slug },
    props: { vehicle }
  }));
}

const { vehicle } = Astro.props;
const vehicleTitle = formatVehicleTitle(vehicle);
const vehicleImages = vehicle.images?.length ? vehicle.images : [vehicle.image];
const hasMultipleImages = vehicleImages.length > 1;
---
<BaseLayout
  title={`${vehicleTitle} - Valade et associés`}
  description={`${vehicleTitle} est listé dans les ventes de véhicules sous contrôle de justice de Valade et associés.`}
  image={vehicle.image}
>
  <section class="section vehicle-detail-hero">
    <div class="wrap vehicle-hero-layout">
      <div>
        <p class="eyebrow">Véhicule</p>
        <h1>{vehicleTitle}</h1>
        <p class="lead">Vente sous contrôle de justice de gré à gré. Les conditions de participation et de paiement sont celles de la page des ventes sabots.</p>
        <div class="actions">
          <a class="button outline" href={`/vente-de-vehicules-operation-sabot-de-denver/#${vehicle.slug}`}>Retour aux véhicules</a>
          <a class="button gold" href={`/vente-de-vehicules-operation-sabot-de-denver/?vehicle=${vehicle.slug}#formulaire-offre`}>Faire une offre</a>
        </div>
      </div>
      <img src={vehicle.image} alt={vehicleTitle} loading="eager" />
    </div>
  </section>

  <section class="section">
    <div class="wrap split">
      <div class="vehicle-carousel" data-vehicle-carousel>
        <div class="carousel-stage">
          {vehicleImages.map((image, index) => (
            <img
              class:list={{ "carousel-image": true, "is-active": index === 0 }}
              src={image}
              alt={`${vehicleTitle} - photo ${index + 1}`}
              loading={index === 0 ? "eager" : "lazy"}
              data-carousel-image
            />
          ))}
          {hasMultipleImages && (
            <div class="carousel-controls" aria-label="Navigation des photos">
              <button class="carousel-button" type="button" aria-label="Photo précédente" data-carousel-prev><span aria-hidden="true">&lsaquo;</span></button>
              <button class="carousel-button" type="button" aria-label="Photo suivante" data-carousel-next><span aria-hidden="true">&rsaquo;</span></button>
            </div>
          )}
        </div>
        {hasMultipleImages && (
          <div class="carousel-thumbs" aria-label="Choisir une photo">
            {vehicleImages.map((image, index) => (
              <button
                class:list={{ "carousel-thumb": true, "is-active": index === 0 }}
                type="button"
                aria-label={`Afficher la photo ${index + 1}`}
                aria-current={index === 0 ? "true" : undefined}
                data-carousel-thumb
              >
                <img src={image} alt="" loading="lazy" />
              </button>
            ))}
          </div>
        )}
      </div>
      <div class="vehicle-info-panel">
        <h2>Informations de vente</h2>
        <dl class="vehicle-details">
          {vehicle.brand && <><dt>Marque</dt><dd>{vehicle.brand}</dd></>}
          {vehicle.model && <><dt>Modèle</dt><dd>{vehicle.model}</dd></>}
          {vehicle.year && <><dt>Année</dt><dd>{vehicle.year}</dd></>}
          {vehicle.vin && <><dt>Numéro d'identification</dt><dd>{vehicle.vin}</dd></>}
          {vehicle.dossier && <><dt>Dossier</dt><dd>{vehicle.dossier}</dd></>}
          {vehicle.keyStatus && <><dt>Clef</dt><dd>{vehicle.keyStatus}</dd></>}
          {vehicle.offerDeadline && <><dt>Date limite</dt><dd>{vehicle.offerDeadline}</dd></>}
        </dl>
        <p>Les ventes ont lieu les mardi et jeudi à 11 h. Les véhicules sont vendus sans garantie légale, aux risques et périls de l'acheteur.</p>
        <div class="notice"><strong>Paiement</strong><p>Le paiement doit être transmis par virement Interac à virements@valade.net dans l'heure suivant l'adjudication.</p></div>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  document.querySelectorAll<HTMLElement>("[data-vehicle-carousel]").forEach((carousel) => {
    const images = Array.from(carousel.querySelectorAll<HTMLImageElement>("[data-carousel-image]"));
    const thumbs = Array.from(carousel.querySelectorAll<HTMLButtonElement>("[data-carousel-thumb]"));
    const prev = carousel.querySelector<HTMLButtonElement>("[data-carousel-prev]");
    const next = carousel.querySelector<HTMLButtonElement>("[data-carousel-next]");
    let activeIndex = 0;

    function showImage(index: number) {
      activeIndex = (index + images.length) % images.length;

      images.forEach((image, imageIndex) => {
        image.classList.toggle("is-active", imageIndex === activeIndex);
      });

      thumbs.forEach((thumb, thumbIndex) => {
        const isActive = thumbIndex === activeIndex;
        thumb.classList.toggle("is-active", isActive);
        thumb.setAttribute("aria-current", isActive ? "true" : "false");
      });
    }

    prev?.addEventListener("click", () => showImage(activeIndex - 1));
    next?.addEventListener("click", () => showImage(activeIndex + 1));
    thumbs.forEach((thumb, index) => thumb.addEventListener("click", () => showImage(index)));
  });
</script>

<style>
  .vehicle-detail-hero {
    background: #101816;
    color: rgba(255,255,255,.82);
  }

  .vehicle-hero-layout {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(280px, .72fr);
    gap: clamp(30px, 6vw, 82px);
    align-items: end;
  }

  .vehicle-detail-hero h1,
  .vehicle-detail-hero .eyebrow {
    color: #fff;
  }

  .vehicle-detail-hero .lead {
    color: rgba(255,255,255,.78);
  }

  .vehicle-hero-layout > img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    border-radius: var(--radius);
  }

  .vehicle-details {
    display: grid;
    grid-template-columns: minmax(150px, max-content) 1fr;
    gap: 0;
    margin: 0 0 24px;
    border-top: 1px solid var(--line);
  }

  .vehicle-details dt,
  .vehicle-details dd {
    margin: 0;
    padding: 13px 0;
    border-bottom: 1px solid var(--line);
  }

  .vehicle-details dt {
    padding-right: 18px;
    color: var(--clay);
    font-size: 12px;
    font-weight: 900;
    text-transform: uppercase;
  }

  .vehicle-details dd {
    color: var(--charcoal);
  }

  .vehicle-info-panel {
    padding: clamp(22px, 4vw, 34px);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background: #fff;
    box-shadow: var(--shadow-soft);
  }

  .vehicle-carousel {
    display: grid;
    gap: 14px;
    align-self: start;
  }

  .carousel-stage {
    position: relative;
    display: grid;
    min-height: 360px;
    overflow: hidden;
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background: #fff;
    box-shadow: var(--shadow-soft);
  }

  .carousel-image {
    grid-area: 1 / 1;
    width: 100%;
    height: 100%;
    max-height: 640px;
    aspect-ratio: 4 / 3;
    object-fit: contain;
    opacity: 0;
    transition: opacity .2s ease;
    pointer-events: none;
  }

  .carousel-image.is-active {
    opacity: 1;
    pointer-events: auto;
  }

  .carousel-controls {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 14px;
    pointer-events: none;
  }

  .carousel-button {
    display: grid;
    place-items: center;
    width: 44px;
    height: 44px;
    padding: 0;
    border: 0;
    border-radius: 50%;
    background: rgba(16, 24, 22, .88);
    color: #fff;
    font-size: 34px;
    line-height: 0;
    cursor: pointer;
    pointer-events: auto;
  }

  .carousel-button span {
    display: block;
    line-height: 1;
    transform: translateY(-2px);
  }

  .carousel-button:hover {
    background: var(--teal);
  }

  .carousel-thumbs {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 10px;
  }

  .carousel-thumb {
    min-width: 0;
    padding: 0;
    border: 3px solid transparent;
    border-radius: 6px;
    background: transparent;
    cursor: pointer;
    overflow: hidden;
  }

  .carousel-thumb.is-active {
    border-color: var(--clay);
  }

  .carousel-thumb img {
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
  }

  @media (max-width: 900px) {
    .vehicle-hero-layout,
    .vehicle-details {
      grid-template-columns: 1fr;
    }

    .vehicle-details dt {
      padding-bottom: 0;
      border-bottom: 0;
    }
  }

  @media (max-width: 720px) {
    .carousel-stage {
      min-height: 260px;
    }

    .carousel-controls {
      padding: 10px;
    }

    .carousel-button {
      width: 38px;
      height: 38px;
      font-size: 30px;
    }
  }
</style>
